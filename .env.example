# Configuration de la base de données Supabase
# Copiez ce fichier en .env et remplissez avec vos vraies valeurs

# Pour une connexion directe (nécessite IPv6)
DB_HOST=db.xxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe


# Clés API Supabase (optionnel pour REST API)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=votre_anon_key
SUPABASE_SERVICE_KEY=votre_service_key


# ========================================
# Configuration LLM (OpenAI ou LM Studio)
# ========================================

# === Option 1: OpenAI API (recommandé) ===
# Définissez USE_OPENAI=true pour utiliser l'API OpenAI
USE_OPENAI=true
OPENAI_API_KEY=sk-proj-VOTRE_CLE_API_OPENAI_ICI
OPENAI_MODEL=gpt-4o-mini
# Modèles disponibles: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4, gpt-3.5-turbo

# === Option 2: LM Studio (local) ===
# Pour utiliser LM Studio à la place :
# 1. Définissez USE_OPENAI=false
# 2. Lancez LM Studio et démarrez un serveur local
# 3. Vérifiez que l'URL correspond à celle de votre serveur LM Studio
# USE_OPENAI=false
# LLM_API_URL=http://localhost:1234/v1/chat/completions
# LLM_API_KEY=  # Optionnel, généralement pas nécessaire pour LM Studio local
